package com.example.onemore.game.kingscup.domain.model

enum class GamePhase {
    WAITING_TO_DRAW, PICK_TARGET, EXECUTING_DISPLAY,
    TOUCH_RACE, PICK_BUDDY, WORD_ROUND, SUBMIT_JACK_RULE, GAME_OVER
}
